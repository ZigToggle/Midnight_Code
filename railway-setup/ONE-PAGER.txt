╔═══════════════════════════════════════════════════════════════════╗
║                  RALPH LOOP - QUICK REFERENCE                     ║
║                   Railway Claude Reconnect                        ║
╚═══════════════════════════════════════════════════════════════════╝

┌───────────────────────────────────────────────────────────────────┐
│ 🚀 SETUP (Run Once)                                               │
└───────────────────────────────────────────────────────────────────┘

  1. Install tmux:
     apt-get update && apt-get install -y tmux

  2. Configure Claude:
     mkdir -p ~/.claude && cat > ~/.claude/settings.json << 'EOF'
     {"permissions":{"defaultMode":"bypassPermissions","allow":["**"]}}
     EOF

  3. Start session:
     tmux new -s ralph

  4. Start the loop:
     while true; do
         echo "Starting Claude at: $(date)"
         claude
         echo "Restarting in 10 seconds..."
         sleep 10
     done

┌───────────────────────────────────────────────────────────────────┐
│ 🔌 DISCONNECT (Keep Claude Running)                               │
└───────────────────────────────────────────────────────────────────┘

  Press:  Ctrl+B  then  D

  ✅ Claude keeps running on Railway
  ✅ Your conversation context is preserved
  ✅ You can safely close your terminal

┌───────────────────────────────────────────────────────────────────┐
│ 🔄 RECONNECT (Get Back to Same Conversation)                      │
└───────────────────────────────────────────────────────────────────┘

  railway ssh && tmux attach -t ralph

  ✅ Same conversation window
  ✅ All context preserved
  ✅ Continue where you left off

┌───────────────────────────────────────────────────────────────────┐
│ 📋 COMMON COMMANDS                                                │
└───────────────────────────────────────────────────────────────────┘

  Check if running:        tmux list-sessions
  Reconnect:               tmux attach -t ralph
  Disconnect:              Ctrl+B then D
  Kill session:            tmux kill-session -t ralph
  Start new session:       tmux new -s ralph

┌───────────────────────────────────────────────────────────────────┐
│ 💡 KEY CONCEPT                                                    │
└───────────────────────────────────────────────────────────────────┘

  tmux = Persistent window on the server

  When you disconnect → tmux keeps Claude running
  When you reconnect → You're back in the SAME session

  Your Computer              Railway Server
       │                          │
       │────── SSH ────────>  [tmux: ralph]
       │                      [Claude running]
       │                          │
   Close Terminal                 │
       │                      [Still running!]
       │                          │
       │────── SSH ────────>  [tmux: ralph]
       │────── Attach ─────>  [Same Claude!]

┌───────────────────────────────────────────────────────────────────┐
│ 🆘 TROUBLESHOOTING                                                │
└───────────────────────────────────────────────────────────────────┘

  Session not found?
    → tmux list-sessions (check if it exists)
    → tmux new -s ralph (start new one)

  Can't attach?
    → tmux kill-session -t ralph (kill old)
    → tmux new -s ralph (start fresh)

  Claude not responding?
    → Ctrl+C to stop
    → Restart the while loop

┌───────────────────────────────────────────────────────────────────┐
│ ✅ REMEMBER                                                       │
└───────────────────────────────────────────────────────────────────┘

  • Always detach with Ctrl+B then D (not Ctrl+C)
  • Reconnect command: tmux attach -t ralph
  • Context is preserved when you detach properly
  • Auto-restart saves you from crashes

╔═══════════════════════════════════════════════════════════════════╗
║  Save this file! Print it! Screenshot it!                         ║
║  You'll use "tmux attach -t ralph" all the time.                  ║
╚═══════════════════════════════════════════════════════════════════╝
